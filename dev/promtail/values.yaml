# Promtail v6.x chart overrides for dev / minikube
resources:
  requests:
    memory: "64Mi"
    cpu: "50m"
  limits:
    memory: "128Mi"
    cpu: "200m"

config:
  clients:
    - url: http://loki.observability.svc.cluster.local:3100/loki/api/v1/push
