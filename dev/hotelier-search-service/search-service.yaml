apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-service
  namespace: hotelier
  labels:
    app: search-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: search-service
  template:
    metadata:
      labels:
        app: search-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: search-service
        image: hotelier-search-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Development"
        - name: ASPNETCORE_URLS
          value: "http://+:8080"
        - name: MongoDB__ConnectionString
          value: "mongodb://mongodb.databases.svc.cluster.local:27017"
        - name: MongoDB__Database
          value: "hotelier_search"
        - name: Rabbit__Host
          value: "rabbitmq.databases.svc.cluster.local"
        resources:
          requests:
            cpu: 250m
            memory: 256Mi
          limits:
            cpu: 1000m
            memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: hotelier
spec:
  selector:
    app: search-service
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
