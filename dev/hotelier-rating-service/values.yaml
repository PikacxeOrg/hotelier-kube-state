replicaCount: 1

image:
  repository: pikaac/rating-service
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 80

resources: {}

ingress:
  enabled: true
  host: "minikube.local"
  path: "/rating"

serviceMonitor:
  enabled: true
  interval: 15s
